import {View, Text} from 'react-native';
import React, {memo} from 'react';

const RadioButtonOption = memo(() => {
  return (
    <View>
      <Text>RadioButtonOption</Text>
    </View>
  );
});

export default RadioButtonOption;
